#ifndef _COMMON_H
    #define _COMMON_H
#endif

// Prints an array list of integers, separated by space.
void print_array_int(int*, int);

// Swap values of two int pointers
void swap_values_int(int*, int*);

// Populate array with random numbers
void populate_array_with_randoms_int(int*, int, int, int);

// Checks if array elements are sorted
int is_array_sorted_int(int*, int);
